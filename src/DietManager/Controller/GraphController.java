package DietManager.Controller;


import DietManager.Model.Entry.Entry;


/**
 * GraphController
 * In this class, the user is able to ???
 */
public class GraphController {


    public GraphController() {

    }

    /**
     * When called,
     *
     * @param properties the attributes associated with the newly created Entry
     */
    public void createEntry(String[] properties) {

    }

    /**
     * When called, this will retrieve and return a specific entry to the user.
     *
     * @param entryToRetrieve the entry we want to return from the model(only if it exists already)
     * @return Entry the entry selected by the user that we need to retrieve
     */
    public Entry retrieveEntry(String entryToRetrieve) {
        return null;
    }
}
